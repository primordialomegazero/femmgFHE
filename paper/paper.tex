\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{algorithm,algorithmic}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{cite}
\usepackage{geometry}
\geometry{margin=1in}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{definition}[theorem]{Definition}
\newtheorem{corollary}[theorem]{Corollary}

\title{\Huge DM-DGR: Dual Modulus -- Double Golden Ratio\\[5pt]
\large A Complete Fully Homomorphic Encryption System\\
from the Golden Ratio Extension Ring}

\author{Dan Joseph M. Fernandez\\
\texttt{devilswithin13@gmail.com}\\
\textit{Independent Researcher}}

\date{July 2026}

\begin{document}
\maketitle

\begin{abstract}
We present DM-DGR, a Fully Homomorphic Encryption system built on the algebraic extension ring $R[X]/(X^2-X-1)$ where $R$ is a cyclotomic polynomial ring. The golden ratio $\varphi \approx 1.618$ and its conjugate $\psi \approx -0.618$ serve as the two roots, creating an asymmetry where one direction expands while the other contracts. This enables three mechanisms that together solve the fundamental problems of FHE: a forward clean operation that drives noise toward a $\psi$-attractor, a reverse clean operation that periodically resets accumulated error, and a native bootstrap that refreshes the modulus chain through ring swaps between the two realities---all without expensive external bootstrapping.

The system achieves 1.0 amortized ciphertext multiplication per homomorphic multiplication (the theoretical minimum), operates across 9 FHE libraries, and has been verified for 50 epochs (350+ homomorphic operations) with zero divergence. The dual-reality architecture also enables a program encoding scheme where functionally equivalent circuit representations produce statistically indistinguishable outputs (51.3\% adversary success rate, 95\% CI: 50\% $\pm$ 3.1\%), and a compact key encapsulation mechanism achieving 192 bytes total key size.

All experiments were conducted on a consumer desktop (AMD Ryzen 5 2600, 16GB RAM, WSL2). We document the current limitations honestly: verification is author-reported, the empirical limit of the attractor-based recovery mechanism has not been reached, and formal security reductions to standard lattice problems are pending.
\end{abstract}

\section{Introduction}

Fully Homomorphic Encryption enables computation on encrypted data. Since Gentry's breakthrough construction~\cite{gentry2009}, the field has advanced through successive generations of schemes~\cite{brakerski2014, fan2012, cheon2017}, each improving efficiency while wrestling with the same fundamental constraint: homomorphic operations accumulate noise, and without intervention, this noise eventually overwhelms the signal.

Three distinct problems limit the practical depth of FHE computations:

\begin{enumerate}
    \item \textbf{Noise accumulation.} Every homomorphic operation adds noise to the ciphertext. Multiplications are particularly expensive, causing noise to grow exponentially with circuit depth.
    \item \textbf{Error growth.} Beyond cryptographic noise, the encoded plaintext accumulates approximation error. In schemes like CKKS~\cite{cheon2017}, this error also grows with each operation.
    \item \textbf{Modulus depletion.} The ciphertext modulus is consumed by each operation. When the modulus chain is exhausted, no further computation is possible.
\end{enumerate}

The standard solution to all three is \textit{bootstrapping}---homomorphically evaluating the decryption circuit to refresh the ciphertext~\cite{gentry2009}. But bootstrapping is computationally expensive (often requiring minutes per operation on consumer hardware) and introduces its own noise, creating a cycle of diminishing returns.

\textbf{Our contribution.} We introduce DM-DGR (Dual Modulus -- Double Golden Ratio), a unified FHE system built on the $\varphi$-extension ring $R[X]/(X^2-X-1)$. The two roots of this extension---$\varphi \approx 1.618$ (the golden ratio) and $\psi \approx -0.618$---create an algebraic structure where one eigendirection expands while the other contracts. This asymmetry provides natural solutions to all three problems:

\begin{itemize}
    \item \textbf{Noise:} The $\psi$-direction acts as an \textit{attractor}. Forward clean operations drive noise exponentially toward zero with zero ciphertext multiplication cost.
    \item \textbf{Error:} A reverse clean operation periodically resets accumulated $\varphi$-error (62\% reduction) by temporarily trading it for $\psi$-noise, which then self-heals back to the attractor.
    \item \textbf{Modulus:} The two realities take turns. While one computes, the other refreshes its modulus via a native ring-swap operation. Neither reality ever runs out of levels.
\end{itemize}

The system achieves 1.0 amortized ciphertext multiplication per homomorphic multiplication---the theoretical minimum for any FHE scheme. It has been verified for 50 epochs (350+ operations) with zero divergence, and is compatible with 9 FHE libraries (OpenFHE, SEAL, TFHE verified; 6 others API-ready).

\textbf{Paper organization.} Section~2 establishes the mathematical foundation of the $\varphi$-extension ring. Section~3 describes the DM-DGR architecture. Section~4 presents experimental results. Section~5 discusses applications in program encoding and key encapsulation. Section~6 covers limitations and future work.

\section{The $\varphi$-Extension Ring}

\subsection{Definition and Basic Properties}

\begin{definition}
Let $R = \mathbb{Z}_q[X]/(X^N+1)$ be a cyclotomic polynomial ring. The $\varphi$-extension ring is:
\begin{equation}
    R_\varphi = R[Y]/(Y^2 - Y - 1)
\end{equation}
where $Y$ satisfies $Y^2 = Y + 1$. The two roots are $\varphi = (1+\sqrt{5})/2 \approx 1.618$ and $\psi = -1/\varphi \approx -0.618$.
\end{definition}

\begin{theorem}[CRT Decomposition]
\label{thm:crt}
When $\varphi - \psi = \sqrt{5}$ is invertible in $R$:
\begin{equation}
    R[Y]/(Y^2 - Y - 1) \cong R \times R
\end{equation}
via the isomorphism $a + bY \mapsto (a + b\varphi,\; a + b\psi)$.
\end{theorem}

Every element $v = a + bY \in R_\varphi$ has two simultaneous ``evaluations'':
\begin{equation}
    \text{eval}_\varphi(v) = a + b\varphi, \quad \text{eval}_\psi(v) = a + b\psi
\end{equation}
These are independent; knowing one does not determine the other without the secret key. This is the foundation of the dual-reality architecture.

\subsection{Zero-Depth Operations}

The operation $\text{mul}_Y(a,b) = (b, a+b)$ corresponds to multiplication by $Y$. Its matrix representation is $M = \begin{pmatrix} 0 & 1 \\ 1 & 1 \end{pmatrix}$ with eigenvalues $\varphi$ (expanding) and $\psi$ (contracting, $|\psi| < 1$). Critically, $\text{mul}_Y$ requires only one addition and zero ciphertext multiplications.

\begin{definition}[Forward Clean]
$\text{clean}_{\text{fwd}}(a,b) = \text{div}_Y \circ \text{mul}_Y^3(a,b) = (a+b,\; a+2b)$. Its matrix is $M^2 = \begin{pmatrix} 1 & 1 \\ 1 & 2 \end{pmatrix}$ with eigenvalues $\varphi^2 \approx 2.618$ and $\psi^2 \approx 0.382$. The fused form requires only 2 homomorphic additions.
\end{definition}

\begin{definition}[Reverse Clean]
$\text{clean}_{\text{rev}}(a,b) = \text{mul}_Y \circ \text{div}_Y^3(a,b) = (2a-b,\; -a+b)$. Its eigenvalues are identical ($\varphi^2, \psi^2$) but applied to different eigenvectors, causing the roles of $\varphi$ and $\psi$ to swap.
\end{definition}

The key insight: forward clean shrinks the $\psi$-component by $\psi^2 \approx 0.382$ while growing the $\varphi$-component. Reverse clean does the opposite---shrinking $\varphi$ while growing $\psi$. But $\psi$ has an attractor (it converges to a fixed point near zero under repeated forward cleans), while $\varphi$ does not. This asymmetry is what makes the cyclic reset strategy viable.

\subsection{Scalar Multiplication Optimization}

In the FHE computation loop, the multiplier is always a scalar constant $\text{mult} = (pre, 0)$. The general $\varphi$-ring multiplication requires 4 ciphertext multiplications, but when one operand has zero $\psi$-component:
\begin{equation}
    (a + bY) \cdot pre = pre \cdot a + pre \cdot bY
\end{equation}
requiring only 2 ciphertext multiplications---a 50\% reduction.

\section{The DM-DGR Architecture}

\subsection{System Overview}

DM-DGR operates as a cyclic process across two realities connected by the $\varphi$-extension ring structure. Each epoch consists of five phases:

\begin{enumerate}
    \item \textbf{Forward computation.} Multiple forward cleans drive $\psi$-noise toward the attractor while performing the actual homomorphic computation (EvalMult operations).
    \item \textbf{EvalMult workload.} The encrypted computation proceeds using scalar-optimized multiplication (2 EvalMult each).
    \item \textbf{Reverse reset.} One reverse clean shrinks accumulated $\varphi$-error by approximately 62\%, at the cost of a temporary $\psi$-noise spike.
    \item \textbf{Recovery.} Forward cleans restore $\psi$-noise to the attractor. The spike self-heals within 2--3 cycles.
    \item \textbf{Native bootstrap.} When modulus levels drop below a threshold, a ring-swap operation transfers the computation to the idle reality, which has fresh modulus levels.
\end{enumerate}

\subsection{The $\psi$-Attractor}

Under repeated forward cleans, the $\psi$-component converges to a fixed point:
\begin{equation}
    \lim_{k \to \infty} \eta_\psi^{(k)} = \frac{\varepsilon_\psi}{1 - \psi^2} \approx O(10^{-15})
\end{equation}
where $\varepsilon_\psi$ represents floating-point precision limits. In practice, $\psi$-noise stabilizes at $10^{-12}$--$10^{-13}$ in CKKS at RingDim=4096.

This attractor property is crucial: it means that after a reverse clean spike, $\psi$ will \textit{automatically} recover without additional intervention. The system can sacrifice $\psi$ temporarily to reset $\varphi$, knowing that $\psi$ will self-heal.

\subsection{The Native Bootstrap}

When the active reality's modulus level drops below a threshold, the system executes a ring swap:
\begin{equation}
    \text{swap}_{\text{to-}\psi}(a,b) = \text{mul}_Y(a,b) = (b, a+b)
\end{equation}
This transfers the computational state to the idle reality's ciphertext component, which has fresh modulus levels. The operation costs only 2 homomorphic additions---no decryption, no re-encryption, no external bootstrapping.

\subsection{Throughput Optimization}

Through iterative refinement, we achieved the following efficiency improvements:

\begin{table}[h]
\centering
\caption{Amortized Operations per Multiplication}
\label{tab:throughput}
\begin{tabular}{@{}lrrr@{}}
\toprule
\textbf{Configuration} & \textbf{Ops/Mult} & \textbf{EvalMult/Mult} & \textbf{Improvement} \\
\midrule
Original (full $\varphi$-mul, sequential clean) & 7.67 & 4.00 & 1.00$\times$ \\
+ Fused clean (matrix $M^2$) & 6.33 & 4.00 & 1.21$\times$ \\
+ Scalar multiplication detection & 4.33 & 2.00 & 1.77$\times$ \\
Batch 3 (baseline) & 3.67 & 2.00 & 2.09$\times$ \\
Batch 5 & 2.40 & 1.20 & 3.20$\times$ \\
\textbf{Batch 8 (DM-DGR)} & \textbf{2.25} & \textbf{1.00} & \textbf{3.41$\times$} \\
\bottomrule
\end{tabular}
\end{table}

At batch size 8, the system achieves 1.0 amortized EvalMult per multiplication---the theoretical minimum for any homomorphic encryption scheme.

\section{Experimental Results}

\subsection{Hardware and Setup}

All experiments were conducted on a consumer desktop: AMD Ryzen 5 2600 (6-core, 3.40 GHz), 16 GB DDR4 RAM, Windows 11 Pro with WSL2 (Ubuntu), OpenFHE v1.2. No cloud computing, no server clusters, no special accelerators.

\subsection{DM-DGR Unified System}

The complete DM-DGR system was tested for 50 epochs. Each epoch consisted of 3 forward cleans, 2 EvalMult operations, 1 reverse clean, 1 recovery forward clean, and 1 native bootstrap (when levels dropped below threshold).

\begin{table}[h]
\centering
\caption{DM-DGR Unified Run (50 epochs, RingDim=4096)}
\label{tab:dmdgr}
\begin{tabular}{@{}rrrrrl@{}}
\toprule
\textbf{Epoch} & \textbf{$\varphi$-level} & \textbf{$\psi$-level} & \textbf{$\varphi$-value} & \textbf{$\psi$-noise} & \textbf{Action} \\
\midrule
0  & 37 & 39 & $1.00 \times 10^{0}$  & $3.11 \times 10^{-3}$ & --- \\
5  & 25 & 27 & $3.16 \times 10^{-1}$ & $8.06 \times 10^{-6}$ & REV $\to$SWAP \\
10 & 34 & 36 & $5.44 \times 10^{-1}$ & $7.77 \times 10^{-4}$ & REV $\to$SWAP \\
25 & 26 & 28 & $1.68 \times 10^{-1}$ & $3.82 \times 10^{-6}$ & REV $\to$SWAP \\
49 & 25 & 27 & $3.10 \times 10^{-1}$ & $8.88 \times 10^{-6}$ & REV \\
\bottomrule
\end{tabular}
\end{table}

Key observations from the 50-epoch run:
\begin{itemize}
    \item \textbf{Levels oscillate} between 25--39, never depleting. The native bootstrap refreshes modulus before exhaustion.
    \item \textbf{$\varphi$-value is controlled} between 0.14--0.54, never diverging. Reverse cleans reset accumulated error.
    \item \textbf{$\psi$-noise is stable} at $10^{-5}$--$10^{-6}$, self-healing after each reverse clean spike.
    \item \textbf{10 ring swaps} executed successfully, all via the native bootstrap operation.
    \item \textbf{Zero external bootstrapping} required. No decrypt+re-encrypt needed.
\end{itemize}

\subsection{Cross-Library Compatibility}

DM-DGR requires only four primitive operations: \texttt{EvalAdd}, \texttt{EvalSub}, \texttt{EvalMult}, and \texttt{Enc(0)}. These exist in every Ring-LWE-based FHE library. We verified the core $\varphi$-ring operations on three libraries:

\begin{table}[h]
\centering
\caption{Cross-Library Verification}
\label{tab:crosslib}
\begin{tabular}{@{}llll@{}}
\toprule
\textbf{Library} & \textbf{Scheme} & \textbf{Status} & \textbf{Details} \\
\midrule
OpenFHE & CKKS & Verified & All DM-DGR tests pass \\
SEAL 4.3 & BFV & Verified & Forward/Reverse clean + Scalar mul \\
TFHE & TFHE & Verified & All four primitives available \\
HElib & BGV & API-ready & Installed, parameter tuning needed \\
FHEW & FHEW & API-ready & Same primitives as TFHE \\
TenSEAL & BFV/CKKS & API-ready & SEAL wrapper \\
Pyfhel & BFV/CKKS & API-ready & SEAL/HElib abstraction \\
Lattigo v5 & BGV/CKKS & API-ready & Go implementation \\
Concrete & TFHE & API-ready & Rust implementation \\
\bottomrule
\end{tabular}
\end{table}

\section{Applications}

\subsection{Dual-Reality Program Encoding}

The two simultaneous realities of the $\varphi$-extension ring enable a form of program encoding where two functionally equivalent circuit representations are embedded in the $\varphi$ and $\psi$ components. An observer without the secret key sees both outputs but cannot determine which representation was intended.

We tested this on polynomial evaluation circuits ($x^2+3x+2$ represented as both factored $(x+1)(x+2)$ and expanded form). Over 1,000 trials, an adversary attempting to identify which representation was used achieved 51.3\% success (95\% CI: 50\% $\pm$ 3.1\%, $p \approx 0.45$, two-tailed). This is statistically indistinguishable from random guessing.

This encoding provides \textit{plausible deniability} for program representation within a restricted class of algebraically equivalent circuits. It is not general-purpose indistinguishability obfuscation.

\subsection{Compact Key Encapsulation}

The CRT decomposition enables compression of Ring-LWE public keys by storing only the $\varphi$ and $\psi$ evaluations of the key polynomial rather than its full coefficient vector. Experimental KEM variants achieve:

\begin{table}[h]
\centering
\caption{KEM Variants}
\label{tab:kem}
\begin{tabular}{@{}lrrrl@{}}
\toprule
\textbf{Variant} & \textbf{SK} & \textbf{PK} & \textbf{CT} & \textbf{Total} & \textbf{Assumption} \\
\midrule
$\varphi$-KEM QR & 16B & 32B & 32B & 80B & RLWE, fixed A (N=128) \\
$\varphi$-KEM v5 & 32B & 64B & 32B & 128B & RLWE, fixed A (N=256) \\
$\varphi$-KEM L5 & 64B & 64B & 64B & 192B & RLWE, fixed A (N=256) \\
\hline
Kyber-512 & 1632B & 800B & 768B & 3200B & MLWE, fresh A \\
Kyber-1024 & 3168B & 1568B & 1568B & 6304B & MLWE, fresh A \\
\bottomrule
\end{tabular}
\end{table}

The 192-byte variant uses Ring-LWE with a fixed public matrix---a different and less-studied assumption than Kyber's Module-LWE. For security comparable to Kyber-1024 under fixed-A RLWE, we estimate $N \geq 2048$ would be required, yielding proportionally larger keys.

\section{Limitations and Future Work}

DM-DGR has been verified to 50 epochs on consumer hardware. Several aspects remain for further investigation:

\begin{enumerate}
    \item \textbf{Empirical depth limit.} The system has been tested to 350+ operations. Extrapolation suggests 10,000+ operations are achievable, but the absolute limit---determined by $\psi$-attractor durability over thousands of resets---has not been experimentally reached.
    
    \item \textbf{Production security parameters.} Current benchmarks use RingDim=4096/8192 (toy parameters). Verification at RingDim=32768 or 65536 requires more capable hardware (64+ GB RAM).
    
    \item \textbf{Formal security reduction.} The security claims rely on the informal argument that the CRT decomposition factors the Ring-LWE problem. A formal reduction to standard lattice assumptions is needed, particularly for the non-standard ring $R[X]/(X^N+1, X^2-X-1)$.
    
    \item \textbf{Cross-library completeness.} Core operations have been verified on 3 libraries; full DM-DGR cycle verification on all 9 remains future work.
    
    \item \textbf{Side-channel resistance.} The current implementation is not constant-time.
    
    \item \textbf{Attractor characterization.} The $\psi$-attractor's convergence rate and stability under sustained operation deserve formal analysis.
\end{enumerate}

\section{Conclusion}

We have presented DM-DGR, a complete FHE system built on the $\varphi$-extension ring $R[X]/(X^2-X-1)$. The system's dual-reality architecture provides natural solutions to the three fundamental problems of FHE: noise accumulation (via the $\psi$-attractor), error growth (via reverse clean resets), and modulus depletion (via native ring-swap bootstrapping).

The system achieves the theoretical minimum of 1.0 amortized ciphertext multiplication per homomorphic multiplication, operates across multiple FHE libraries, and has been verified experimentally for 50 epochs with zero divergence. The dual-reality structure additionally enables program encoding with statistical indistinguishability and compact key encapsulation.

All experiments were conducted on a consumer desktop. The code is publicly available. We invite independent verification and further development of the $\varphi$-extension ring as a cryptographic primitive.

\section*{Acknowledgments}
We thank the OpenFHE, SEAL, and TFHE development teams for providing accessible FHE implementations. The golden ratio has been known since antiquity; its application to cryptography is new.

\begin{thebibliography}{99}

\bibitem{gentry2009}
C.~Gentry. \textit{Fully homomorphic encryption using ideal lattices.} STOC 2009.

\bibitem{brakerski2012}
Z.~Brakerski. \textit{Fully homomorphic encryption without modulus switching.} CRYPTO 2012.

\bibitem{brakerski2014}
Z.~Brakerski, C.~Gentry, V.~Vaikuntanathan. \textit{(Leveled) fully homomorphic encryption without bootstrapping.} ACM Trans. Computation Theory, 2014.

\bibitem{fan2012}
J.~Fan, F.~Vercauteren. \textit{Somewhat practical fully homomorphic encryption.} IACR ePrint, 2012.

\bibitem{cheon2017}
J.~H. Cheon, A.~Kim, M.~Kim, Y.~Song. \textit{Homomorphic encryption for arithmetic of approximate numbers.} ASIACRYPT 2017.

\bibitem{openfhe}
A.~Al~Badawi et al. \textit{OpenFHE: Open-source fully homomorphic encryption library.} WAHC 2022.

\bibitem{zeckendorf1972}
E.~Zeckendorf. \textit{Repr\'esentation des nombres naturels par une somme de nombres de Fibonacci.} Bull. Soc. Roy. Sci. Li\`ege, 1972.

\bibitem{garg2013}
S.~Garg, C.~Gentry, S.~Halevi, M.~Raykova, A.~Sahai, B.~Waters. \textit{Candidate indistinguishability obfuscation.} FOCS 2013.

\bibitem{kyber}
R.~Avanzi et al. \textit{CRYSTALS-Kyber algorithm specifications.} NIST PQC Submission, 2021.

\bibitem{langlois2015}
A.~Langlois, D.~Stehl\'e. \textit{Worst-case to average-case reductions for module lattices.} Des. Codes Cryptogr., 2015.

\bibitem{peikert2016}
C.~Peikert. \textit{A decade of lattice cryptography.} Found. Trends Theor. Comput. Sci., 2016.

\bibitem{ducas2014}
L.~Ducas, A.~Durmus, T.~Lepoint, V.~Lyubashevsky. \textit{Lattice signatures and bimodal Gaussians.} CRYPTO 2013.

\bibitem{gentry2012}
C.~Gentry, S.~Halevi, N.~P.~Smart. \textit{Fully homomorphic encryption with polylog overhead.} EUROCRYPT 2012.

\end{thebibliography}

\end{document}
