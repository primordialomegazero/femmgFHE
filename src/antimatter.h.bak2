#pragma once
// ANTI-MATTER DISABLED FOR DOCKER TESTING

static inline int antimatter_check(const char* ip, const char* headers) {
    return 0; // Always allow
}

static inline int antimatter_rate_limit(const char* ip) {
    return 0; // No rate limiting
}
